# 📖 Подробная инструкция по запуску и тестированию

## 🎯 Что было создано

Создано современное десктопное приложение на React + TypeScript с красивым дизайном страницы авторизации и регистрации. Приложение использует:

- **React 18** с TypeScript
- **Vite** для быстрой разработки
- **Tailwind CSS** для стилизации
- **React Router** для навигации
- **Axios** для работы с API

## 📦 Шаг 1: Установка зависимостей

Откройте терминал в папке `frontend` и выполните:

```bash
cd frontend
npm install
```

Это установит все необходимые зависимости проекта. Процесс может занять 1-2 минуты.

**Альтернативные менеджеры пакетов:**
- `yarn install` (если используете Yarn)
- `pnpm install` (если используете pnpm)

## 🚀 Шаг 2: Запуск backend сервера

**ВАЖНО:** Перед запуском frontend необходимо запустить backend сервер!

В корневой папке проекта (не в `frontend`) выполните:

```bash
# Активируйте виртуальное окружение (если еще не активировано)
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac

# Запустите сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Сервер должен запуститься и быть доступен на `http://localhost:8000`.

Проверить, что сервер работает, можно открыв в браузере:
- http://localhost:8000/docs (Swagger документация)

## 🎨 Шаг 3: Запуск frontend приложения

В новом терминале перейдите в папку `frontend` и выполните:

```bash
cd frontend
npm run dev
```

Вы должны увидеть сообщение:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

Приложение будет доступно по адресу: **http://localhost:3000**

## 🧪 Шаг 4: Тестирование приложения

### Тест 1: Проверка главной страницы

1. Откройте браузер и перейдите на http://localhost:3000
2. Вы должны увидеть страницу входа с:
   - Логотипом приложения вверху
   - Заголовком "Yooo, welcome back!"
   - Полем для ввода email (на самом деле номер телефона)
   - Полем для ввода пароля
   - Кнопкой "Sign in"
   - Ссылкой "Sign up for free"

### Тест 2: Регистрация нового пользователя

1. На странице входа нажмите на ссылку **"Sign up for free"**
2. Вы перейдете на страницу регистрации
3. Заполните форму:
   - **Phone number**: `+79991234567` (или любой другой номер)
   - **First name**: `Иван` (обязательное поле)
   - **Last name**: `Иванов` (опционально)
   - **Username**: `ivan_user` (опционально)
   - **Password**: `password123` (минимум 6 символов)
   - **Confirm password**: `password123` (должен совпадать с паролем)
4. Нажмите кнопку **"Sign up"**
5. Если регистрация успешна, вы будете автоматически перенаправлены на страницу Dashboard

**Ожидаемый результат:**
- Форма отправляется
- Появляется индикатор загрузки
- После успешной регистрации происходит автоматический вход
- Вы видите страницу Dashboard с приветствием

**Если возникла ошибка:**
- Проверьте, что backend сервер запущен
- Проверьте, что номер телефона уникален (если пользователь уже существует, используйте другой номер)
- Проверьте консоль браузера (F12) на наличие ошибок

### Тест 3: Вход в систему

1. Если вы уже зарегистрированы, вернитесь на страницу входа
2. Введите данные:
   - **Your email**: `+79991234567` (номер телефона, который использовали при регистрации)
   - **Password**: `password123` (пароль, который использовали при регистрации)
3. Нажмите кнопку **"Sign in"**

**Ожидаемый результат:**
- Форма отправляется
- Появляется индикатор загрузки
- После успешного входа вы перенаправляетесь на Dashboard

**Если возникла ошибка:**
- Проверьте правильность номера телефона и пароля
- Убедитесь, что пользователь существует в базе данных
- Проверьте консоль браузера на наличие ошибок

### Тест 4: Выход из системы

1. На странице Dashboard нажмите кнопку **"Logout"**
2. Вы должны быть перенаправлены на страницу входа

### Тест 5: Защита маршрутов

1. Попробуйте напрямую перейти на http://localhost:3000/dashboard
2. Если вы не авторизованы, вы должны быть автоматически перенаправлены на страницу входа

## 🔍 Проверка работы API

### Через консоль браузера

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network** (Сеть)
3. Выполните вход или регистрацию
4. Вы должны увидеть запросы к API:
   - `POST /api/v1/auth/register` (при регистрации)
   - `POST /api/v1/auth/token` (при входе)

### Через Swagger UI

1. Откройте http://localhost:8000/docs
2. Найдите раздел **Auth**
3. Попробуйте выполнить запросы через Swagger UI

## 🐛 Решение проблем

### Проблема: "Cannot find module" или ошибки импорта

**Решение:**
```bash
cd frontend
rm -rf node_modules package-lock.json  # Linux/Mac
# или
rmdir /s node_modules  # Windows
del package-lock.json  # Windows

npm install
```

### Проблема: Порт 3000 уже занят

**Решение:**
1. Измените порт в файле `frontend/vite.config.ts`:
   ```typescript
   server: {
     port: 3001, // Измените на свободный порт
   }
   ```
2. Или остановите процесс, использующий порт 3000

### Проблема: "Network Error" или ошибки CORS

**Решение:**
1. Убедитесь, что backend запущен на `http://localhost:8000`
2. Проверьте, что в backend настроен CORS для `http://localhost:3000`
3. Проверьте консоль браузера на наличие ошибок

### Проблема: Логотип не отображается

**Решение:**
1. Убедитесь, что файлы находятся в `frontend/public/`:
   - `appicon_gradient.png`
   - `appicon_default.png`
2. Если файлов нет, скопируйте их из папки `assets/` в `frontend/public/`

### Проблема: Страница не загружается

**Решение:**
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что все зависимости установлены: `npm install`
3. Попробуйте очистить кэш браузера (Ctrl+Shift+R)

## 📝 Дополнительные возможности

### Hot Module Replacement (HMR)

При разработке изменения в коде применяются автоматически без перезагрузки страницы. Просто сохраните файл, и изменения появятся в браузере.

### Проверка кода (Linting)

Для проверки качества кода выполните:
```bash
npm run lint
```

### Сборка для production

Для создания production сборки:
```bash
npm run build
```

Собранные файлы будут в папке `dist/`. Для предпросмотра:
```bash
npm run preview
```

## 🎨 Особенности дизайна

- **Темная тема** с градиентным фоном
- **Эффект стекла** (glass effect) для карточек
- **Плавные анимации** и переходы
- **Адаптивный дизайн** для разных размеров экрана
- **Современная типографика**

## ✅ Чек-лист тестирования

- [ ] Backend сервер запущен и доступен
- [ ] Frontend приложение запущено на http://localhost:3000
- [ ] Страница входа отображается корректно
- [ ] Логотип приложения виден
- [ ] Можно перейти на страницу регистрации
- [ ] Регистрация нового пользователя работает
- [ ] Вход в систему работает
- [ ] Dashboard отображается после входа
- [ ] Выход из системы работает
- [ ] Защищенные маршруты работают корректно

## 🎉 Готово!

Если все тесты пройдены успешно, приложение готово к использованию и дальнейшей разработке!

---

**Приятной работы! 🚀**

