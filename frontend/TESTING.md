# 🧪 Инструкция по тестированию

## Быстрый старт для тестирования Electron приложения

### Шаг 1: Убедитесь, что бэкенд запущен

```bash
# В корне проекта
# Бэкенд должен быть на http://localhost:8000
```

### Шаг 2: Запустите Electron приложение

```bash
cd frontend
npm run dev:electron
```

Это автоматически:
- ✅ Запустит Vite dev server
- ✅ Дождется готовности сервера
- ✅ Откроет Electron окно с приложением

### Шаг 3: Тестирование

Electron окно откроется автоматически с приложением.

## 🔄 Альтернативный запуск

Если Vite dev server уже запущен в другом терминале:

```bash
cd frontend
npm run electron
```

## ✅ Что проверить

### В Electron приложении:

- [ ] Приложение открывается без ошибок
- [ ] Интерфейс отображается корректно
- [ ] Навигация работает (используется HashRouter: `#/path`)
- [ ] API запросы работают
- [ ] Регистрация/авторизация работает
- [ ] Загрузка аватара работает
- [ ] DevTools открыты (в dev режиме)
- [ ] TitleBar работает (минимизация, максимизация, закрытие)

## 🐛 Отладка

### Если Electron не запускается:

1. Проверьте, что Vite запущен: `http://localhost:3000`
2. Проверьте консоль на ошибки
3. Убедитесь, что бэкенд запущен на `http://localhost:8000`

### Если видите белый экран:

1. Откройте DevTools (F12 или автоматически в dev режиме)
2. Проверьте консоль на ошибки
3. Проверьте Network tab - работают ли API запросы

### Если навигация не работает:

- В Electron используется HashRouter (`#/login` вместо `/login`)
- Это нормальное поведение для Electron
- Проверьте, что URL начинается с `#/`

## 📦 Сборка для production

### Сборка Electron приложения:
```bash
npm run electron:dist
```

Установщик будет в папке `release/`

## 💡 Полезные команды

```bash
# Запуск Electron с hot reload (рекомендуется)
npm run dev:electron

# Только Electron (если Vite уже запущен)
npm run electron

# Сборка frontend (требуется для production)
npm run build

# Сборка Electron (без установщика, для тестирования)
npm run electron:pack

# Сборка Electron (с установщиком)
npm run electron:dist
```

