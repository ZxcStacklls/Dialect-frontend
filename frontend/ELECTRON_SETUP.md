# Настройка и тестирование Electron приложения

## 📋 Требования

- Node.js 18+ 
- npm или yarn
- Бэкенд сервер должен быть запущен на `http://localhost:8000`

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd frontend
npm install
```

### 2. Тестирование Web версии

```bash
npm run dev
```

Откройте браузер: `http://localhost:3000`

### 3. Тестирование Desktop версии (Electron)

#### Вариант A: Параллельный запуск (рекомендуется)

Запускает Vite dev server и Electron одновременно:

```bash
npm run dev:electron
```

Это автоматически:
1. Запустит Vite dev server на порту 3000
2. Дождется готовности сервера
3. Запустит Electron приложение

#### Вариант B: Ручной запуск

Если Vite уже запущен в другом терминале:

```bash
# Терминал 1: Запуск Vite
npm run dev

# Терминал 2: Запуск Electron
npm run electron
```

## 🏗️ Сборка для production

### Web версия

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Desktop версия

#### Сборка без установщика (для тестирования)

```bash
npm run electron:pack
```

Результат в папке `release/`

#### Сборка с установщиком

**Windows:**
```bash
npm run electron:dist
```

Создаст установщик `.exe` в папке `release/`

**macOS:**
```bash
npm run electron:dist
```

Создаст `.dmg` файл

**Linux:**
```bash
npm run electron:dist
```

Создаст `.AppImage` файл

## 🔧 Конфигурация

### Изменение порта API

Создайте файл `.env` в папке `frontend/`:

```env
VITE_API_BASE_URL=http://localhost:8000/api
```

### Изменение настроек Electron

Отредактируйте `electron/main.js`:
- Размер окна
- Иконка приложения
- DevTools в production

### Изменение настроек сборки

Отредактируйте `electron-builder.yml` или секцию `build` в `package.json`

## 🐛 Отладка

### DevTools в Electron

В режиме разработки DevTools открываются автоматически.

В production можно добавить в `electron/main.js`:
```javascript
mainWindow.webContents.openDevTools()
```

### Логи

Логи Electron выводятся в консоль терминала, где запущено приложение.

### Проверка платформы в коде

```typescript
import { isElectron, isWeb, getPlatform } from './utils/platform'

if (isElectron()) {
  console.log('Запущено в Electron')
  console.log('Платформа:', getPlatform())
} else {
  console.log('Запущено в браузере')
}
```

## 📝 Доступные скрипты

- `npm run dev` - Запуск Web версии (Vite dev server)
- `npm run dev:electron` - Запуск Electron с hot reload
- `npm run build` - Сборка Web версии
- `npm run electron` - Запуск Electron (требует собранный build или dev server)
- `npm run electron:pack` - Сборка Electron без установщика
- `npm run electron:dist` - Сборка Electron с установщиком

## ⚠️ Важные замечания

1. **API сервер должен быть запущен** перед запуском Electron
2. **В Electron используется HashRouter** вместо BrowserRouter для совместимости
3. **Относительные пути** настроены в `vite.config.ts` (`base: './'`)
4. **Иконки** должны быть в формате PNG для всех платформ

## 🔍 Проверка работы

После запуска `npm run dev:electron`:

1. ✅ Должно открыться окно Electron
2. ✅ Должен загрузиться интерфейс приложения
3. ✅ DevTools должны быть открыты (в dev режиме)
4. ✅ API запросы должны работать
5. ✅ Навигация должна работать корректно

## 🆘 Решение проблем

### Electron не запускается

- Убедитесь, что Vite dev server запущен на порту 3000
- Проверьте, что все зависимости установлены: `npm install`
- Проверьте логи в консоли

### API запросы не работают

- Убедитесь, что бэкенд запущен на `http://localhost:8000`
- Проверьте переменную `VITE_API_BASE_URL` в `.env`

### Окно пустое или белое

- Проверьте консоль DevTools на ошибки
- Убедитесь, что Vite dev server работает
- Проверьте путь в `electron/main.js` (должен быть `http://localhost:3000`)

### Навигация не работает

- В Electron используется HashRouter (`#/path`)
- Это нормальное поведение для Electron

