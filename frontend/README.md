# 🌟 Dialect Frontend — Desktop приложение (Electron)

Десктопное приложение для мессенджера Dialect, построенное на React + TypeScript с использованием Electron, Vite и Tailwind CSS.

**Важно:** Это приложение работает только в Electron. Web-версия не поддерживается.

## 🛠️ Технологический стек

- **React 18** — UI библиотека
- **TypeScript** — типизация
- **Electron** — десктопная платформа
- **Vite** — сборщик и dev-сервер
- **Tailwind CSS** — стилизация
- **React Router** — маршрутизация (HashRouter для Electron)
- **Axios** — HTTP клиент

## 📋 Требования

Перед началом работы убедитесь, что у вас установлены:

- **Node.js** версии 18 или выше
- **npm** или **yarn** или **pnpm**
- **Backend сервер** должен быть запущен на `http://localhost:8000`

## 🚀 Быстрый старт

### 1️⃣ Установка зависимостей

Откройте терминал в папке `frontend` и выполните:

```bash
cd frontend
npm install
```

Или если используете yarn:

```bash
yarn install
```

Или если используете pnpm:

```bash
pnpm install
```

### 2️⃣ Настройка переменных окружения (опционально)

Создайте файл `.env` в папке `frontend` (если нужно изменить URL бэкенда):

```env
VITE_API_BASE_URL=http://localhost:8000/api
```

По умолчанию используется `http://localhost:8000/api`.

### 3️⃣ Запуск приложения в режиме разработки

Для запуска Electron приложения в режиме разработки:

```bash
npm run dev:electron
```

Или запустите отдельно:

1. **Запустите Vite dev-сервер:**
   ```bash
   npm run dev
   ```

2. **В другом терминале запустите Electron:**
   ```bash
   npm run electron
   ```

Приложение откроется в окне Electron. DevTools будут открыты автоматически.

### 4️⃣ Сборка production версии

Для создания production сборки:

```bash
npm run build:electron
```

Или пошагово:

1. **Соберите frontend:**
   ```bash
   npm run build
   ```

2. **Соберите Electron приложение:**
   ```bash
   npm run electron:dist
   ```

Собранные файлы будут в папке `release/`.

## 🧪 Тестирование приложения

### Тестирование страницы авторизации

1. **Убедитесь, что backend запущен:**
   ```bash
   # В корневой папке проекта
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

2. **Запустите Electron приложение:**
   ```bash
   cd frontend
   npm run dev:electron
   ```

3. **Приложение откроется автоматически:**
   - Electron окно откроется с приложением
   - Вы должны увидеть страницу приветствия

### Тестирование регистрации

1. На странице входа нажмите на ссылку **"Sign up for free"**
2. Заполните форму регистрации:
   - **Phone number** — номер телефона (например: `+79991234567`)
   - **First name** — имя (обязательно)
   - **Last name** — фамилия (опционально)
   - **Username** — имя пользователя (опционально)
   - **Password** — пароль (минимум 6 символов)
   - **Confirm password** — подтверждение пароля
3. Нажмите кнопку **"Sign up"**
4. После успешной регистрации вы будете автоматически перенаправлены на Dashboard

### Тестирование входа

1. На странице входа введите:
   - **Your email** — номер телефона (например: `+79991234567`)
   - **Password** — пароль
2. Нажмите кнопку **"Sign in"**
3. После успешного входа вы будете перенаправлены на Dashboard

### Тестирование выхода

1. На странице Dashboard нажмите кнопку **"Logout"**
2. Вы будете перенаправлены на страницу входа

## 📁 Структура проекта

```
frontend/
├── public/                 # Статические файлы (логотипы)
│   ├── appicon_gradient.png
│   └── appicon_default.png
├── src/
│   ├── api/               # API клиент
│   │   ├── client.ts      # Axios конфигурация
│   │   └── auth.ts        # API методы для авторизации
│   ├── components/        # React компоненты
│   │   ├── AppLogo.tsx
│   │   ├── BackButton.tsx
│   │   └── ProtectedRoute.tsx
│   ├── contexts/          # React Context
│   │   └── AuthContext.tsx
│   ├── pages/            # Страницы приложения
│   │   ├── LoginPage.tsx
│   │   ├── SignUpPage.tsx
│   │   └── Dashboard.tsx
│   ├── App.tsx           # Главный компонент
│   ├── main.tsx          # Точка входа
│   └── index.css         # Глобальные стили
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
└── README.md
```

## 🎨 Особенности дизайна

- **Темная тема** — современный темный интерфейс
- **Glass effect** — эффект стекла для карточек
- **Градиентный фон** — красивый градиент от темно-серого к черному
- **Плавные анимации** — переходы и hover эффекты
- **Адаптивный дизайн** — работает на разных размерах экрана
- **Современная типографика** — чистые и читаемые шрифты

## 🔧 Настройка

### Изменение порта dev-сервера

В файле `vite.config.ts` измените:

```typescript
server: {
  port: 3000, // Измените на нужный порт
}
```

**Важно:** Если измените порт, также обновите `electron/main.cjs`:

```javascript
mainWindow.loadURL('http://localhost:НОВЫЙ_ПОРТ')
```

### Изменение URL бэкенда

Создайте файл `.env` в папке `frontend`:

```env
VITE_API_BASE_URL=http://your-backend-url:8000/api
```

Или измените в `src/utils/platform.ts`:

```typescript
return 'http://your-backend-url:8000/api'
```

## 🐛 Решение проблем

### Проблема: "Cannot find module"

**Решение:** Удалите `node_modules` и `package-lock.json`, затем выполните:
```bash
npm install
```

### Проблема: "Port 3000 is already in use"

**Решение:** Измените порт в `vite.config.ts` и обновите `electron/main.cjs`, или остановите процесс, использующий порт 3000.

### Проблема: Electron не открывается или показывает пустой экран

**Решение:** 
1. Убедитесь, что Vite dev-сервер запущен на `http://localhost:3000`
2. Проверьте консоль Electron (DevTools) на наличие ошибок
3. Убедитесь, что все зависимости установлены: `npm install`

### Проблема: "Network Error" при запросах к API

**Решение:** 
1. Убедитесь, что backend запущен на `http://localhost:8000`
2. Проверьте настройки CORS в backend
3. Проверьте переменную окружения `VITE_API_BASE_URL`

### Проблема: Логотип не отображается

**Решение:** Убедитесь, что файлы `appicon_gradient.png` и `appicon_default.png` находятся в папке `frontend/public/`.

## 📝 Дополнительная информация

- **Hot Module Replacement (HMR)** — изменения применяются автоматически без перезагрузки страницы
- **TypeScript** — полная типизация для безопасности кода
- **ESLint** — линтер для проверки качества кода (запуск: `npm run lint`)

## 🚀 Следующие шаги

После успешного запуска и тестирования авторизации, вы можете:

1. Добавить страницу списка чатов
2. Реализовать WebSocket подключение для real-time сообщений
3. Добавить страницу профиля пользователя
4. Реализовать загрузку аватарок
5. Добавить уведомления

---

**Приятной разработки! 🎉**

